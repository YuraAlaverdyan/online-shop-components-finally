import { all } from "axios"
import { DropDown } from "../DropDown"

export const ShowTabs = (props) => {
    return <>
        <div className='d-flex justify-content-center gap-5'>
            {/* <button className='btn btn-primary' onClick={() => props.onShowAll()}>All</button>
            <button className='btn btn-primary' onClick={() => props.onShowClothes()}>Clothes</button>
            <button className='btn btn-primary' onClick={() => props.onShowTech()}>Tech</button>
            <button className='btn btn-primary' onClick={() => props.onShowBooks()}>Books</button> */}
            
        </div>
    </>
}